[package]
name = "janitor-mock"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Janitor's offline Provider: canned demo data, no AWS (ADR 0019)."

[dependencies]
janitor-core = { path = "../janitor-core" }
async-trait = "0.1"
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
